<h3 class="mb-4">Allegati per Terapia</h3>

<p>
  <strong>Data terapia:</strong>
  <%= therapy.data_trattamento.slice(0,10) %> —
  <%= therapy.anagrafica.nome %> <%= therapy.anagrafica.cognome %>
</p>

<hr>

<div class="container py-4">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8">
      <div class="card shadow-sm">
        <div class="card-body">
          <h3 class="card-title mb-3">Allegati per Terapia</h3>
          <p class="mb-4">
            <strong>Data terapia:</strong> <%= therapy.data_trattamento.slice(0,10) %>
            — <%= therapy.anagrafica.nome %> <%= therapy.anagrafica.cognome %>
          </p>

```
      <hr>

      <h5 class="mb-3">Carica nuovo allegato</h5>
      <form method="POST" action="/terapie/<%= therapy.id %>/allegati" enctype="multipart/form-data">
        <div class="input-group mb-4">
          <input type="file" name="allegato" class="form-control" required>
          <button class="btn btn-primary" type="submit">Carica</button>
        </div>
      </form>

      <h5>Allegati esistenti</h5>
      <% if (attachments.length === 0) { %>
        <p class="text-muted">Nessun allegato caricato.</p>
      <% } else { %>
        <div class="row gy-3">
          <% attachments.forEach(a => { %>
            <div class="col-12 col-sm-6">
              <div class="card">
                <img src="<%= a.url %>" class="card-img-top img-fluid" alt="Allegato">
                <div class="card-body d-flex justify-content-between align-items-center">
                  <small class="text-truncate" style="max-width: 150px;"><%= a.url.split('/').pop() %></small>
                  <div>
                    <a href="<%= a.url %>" target="_blank" class="btn btn-sm btn-outline-primary me-2">Apri</a>
                    <form action="/allegati/<%= a.id %>/delete" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-sm btn-outline-danger">Elimina</button>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>

      <div class="mt-4">
        <a href="/fascicoli" class="btn btn-secondary">← Torna a Fascicoli</a>
      </div>
    </div>
  </div>
</div>
```

  </div>
</div>
