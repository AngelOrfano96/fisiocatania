<h3>Registrazione Calciatore</h3>

<% if (typeof message !== 'undefined') { %>
  <div class="alert alert-success"><%= message %></div>
<% } %>

<form method="POST" action="/anagrafica">
  <div class="mb-3"><input name="cognome" class="form-control" placeholder="Cognome" required></div>
  <div class="mb-3"><input name="nome" class="form-control" placeholder="Nome" required></div>
  <div class="mb-3"><input type="date" name="dataNascita" class="form-control" required></div>
  <div class="mb-3"><input name="luogoNascita" class="form-control" placeholder="Luogo di nascita" required></div>
  <div class="mb-3"><input name="cellulare" class="form-control" placeholder="Cellulare" required></div>
  <div class="mb-3"><textarea name="note" class="form-control" placeholder="Note"></textarea></div>
  <button type="submit" class="btn btn-success">Salva</button>
</form>

<hr>
<h4 class="mt-5">Lista Anagrafiche</h4>
<form method="GET" action="/anagrafica" class="row g-2 mb-3">
  <div class="col-md-5">
    <input type="text" name="cognome" class="form-control" placeholder="Filtra per Cognome" value="<%= filters.cognome %>">
  </div>
  <div class="col-md-5">
    <input type="text" name="nome" class="form-control" placeholder="Filtra per Nome" value="<%= filters.nome %>">
  </div>
  <div class="col-md-2">
    <button class="btn btn-primary w-100" type="submit">Cerca</button>
  </div>
</form>

<table id="tabellaGiocatori" class="table table-bordered table-striped align-middle">
  <thead>
    <tr>
      <th>Anagrafica</th>
      <th>Data Nascita</th>
      <th>Comune di Nascita</th>
      <th>Cellulare</th>
      <th>Note</th>
      <th>Modifica</th>
      <th>Elimina</th>
    </tr>
  </thead>
  <tbody>
    <% giocatori.forEach(g => { %>
      <tr data-id="<%= g.id %>">
        <form class="edit-form" method="POST" action="/anagrafica/update/<%= g.id %>">
          <td><input name="nome" class="form-control form-control-sm-plaintext" value="<%= g.nome %>" readonly>
              <input name="cognome" class="form-control form-control-sm-plaintext mt-1" value="<%= g.cognome %>" readonly></td>
          <td><input name="data_nascita" type="date" class="form-control form-control-sm" value="<%= g.data_nascita.toISOString().split('T')[0] %>" readonly></td>
          <td><input name="luogo_nascita" class="form-control form-control-sm" value="<%= g.luogo_nascita %>" readonly></td>
          <td><input name="cellulare" class="form-control form-control-sm" value="<%= g.cellulare %>" readonly></td>
          <td><textarea name="note" class="form-control form-control-sm" readonly><%= g.note %></textarea></td>
          <td>
            <button type="button" class="btn btn-sm btn-warning btn-edit">✏️</button>
            <button type="submit" class="btn btn-sm btn-success d-none btn-save">💾</button>
            <button type="button" class="btn btn-sm btn-secondary d-none btn-cancel">↩️</button>
          </td>
          <td>
            <form method="POST" action="/anagrafica/delete/<%= g.id %>" onsubmit="return confirm('Sei sicuro di voler eliminare?')">
              <button class="btn btn-sm btn-danger">🗑️</button>
            </form>
          </td>
        </form>
      </tr>
    <% }) %>
  </tbody>
</table>

<link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css">
<script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
<script>
  $(document).ready(function () {
    $('#tabellaGiocatori').DataTable({
      pageLength: 10,
      lengthMenu: [5, 10, 20, 50],
      language: {
        url: '//cdn.datatables.net/plug-ins/1.13.6/i18n/it-IT.json'
      },
      columnDefs: [{ orderable: false, targets: [5, 6] }]
    });

    // Comportamento modifica inline
    $('.btn-edit').click(function () {
      const row = $(this).closest('form');
      row.find('input, textarea').prop('readonly', false);
      row.find('.btn-edit').addClass('d-none');
      row.find('.btn-save, .btn-cancel').removeClass('d-none');
    });

    $('.btn-cancel').click(function () {
      location.reload(); // Reload semplice per annullare ogni modifica
    });
  });
</script>
